import Button from "@mui/material/Button";

export default function () {
    return (
        <div>
            <Button variant="contained">样例按钮</Button>
            <div>为所有组件设置dark theme,详情参考src/app/layout.tsx</div>
        </div>
    );
}