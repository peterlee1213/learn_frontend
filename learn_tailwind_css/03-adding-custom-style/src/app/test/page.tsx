export default function () {
    return (
        <div>
            <div className="text-4xl">使用自定义的数值（宽度/高度等等）</div>
            <div className="w-[22px] h-[22px] bg-[#abcdef]"></div>
            <div className="text-gray-400">就是说应当先写mobile页面，然后写sm,然后md,以此类推</div>
        </div>
    );
}